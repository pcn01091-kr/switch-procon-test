/* GamepadViewer - Minimal (L, R, A, B, Left Stick only)
   Layout matches your sketch: L/R rectangles on top, big left stick circle, A/B circles on right
*/

html, body {
  background: transparent !important;
  margin: 0 !important;
  padding: 0 !important;
  overflow: hidden !important;
}

/* Anchor bottom-right (good for 1920x1080) */
.controller.custom{
  position: fixed;
  right: 40px;
  bottom: 40px;

  /* overall overlay size (adjust if you want smaller/larger) */
  width: 360px;
  height: 220px;

  background: transparent !important;
}

/* Hide EVERYTHING first */
.custom * { display: none; }

/* Turn on only the groups we need */
.custom .bumpers,
.custom .bumper,
.custom .abxy,
.custom .button,
.custom .sticks,
.custom .stick { display: block; }

/* ========== Common styles ========== */
.custom .button,
.custom .bumper{
  box-sizing: border-box;
  border: 4px solid rgba(0,0,0,0.95);
  background: rgba(255,255,255,0.02);
}

/* label text */
.custom .button::after,
.custom .bumper::after{
  content: "";
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font: 900 26px/1 sans-serif;
  color: rgba(0,0,0,0.95);
}

/* Pressed = red light */
.custom .button.pressed,
.custom .bumper.pressed{
  border-color: rgba(255, 0, 0, 0.95);
  box-shadow: 0 0 18px rgba(255, 0, 0, 0.9);
  background: rgba(255, 0, 0, 0.10);
}

/* ========== L / R rectangles (top) ========== */
.custom .bumpers{
  position: absolute;
  left: 0;
  top: 0;
  width: 360px;
  height: 70px;
}

/* L */
.custom .bumper.left{
  position: absolute;
  left: 40px;
  top: 10px;
  width: 120px;
  height: 50px;
  border-radius: 0; /* sketch-like rectangle */
}
.custom .bumper.left::after{ content: "L"; }

/* R */
.custom .bumper.right{
  position: absolute;
  right: 40px;
  top: 10px;
  width: 120px;
  height: 50px;
  border-radius: 0; /* sketch-like rectangle */
}
.custom .bumper.right::after{ content: "R"; }

/* ========== A / B (right side circles) ========== */
.custom .abxy{
  position: absolute;
  right: 0;
  top: 70px;
  width: 200px;
  height: 150px;
}

/* hide X/Y completely */
.custom .abxy .x,
.custom .abxy .y{ display: none !important; }

/* A (upper-right) */
.custom .button.a{
  position: absolute;
  right: 55px;
  top: 20px;
  width: 62px;
  height: 62px;
  border-radius: 999px;
}
.custom .button.a::after{ content: "A"; }

/* B (lower-left-ish under A) */
.custom .button.b{
  position: absolute;
  right: 110px;
  top: 85px;
  width: 62px;
  height: 62px;
  border-radius: 999px;
}
.custom .button.b::after{ content: "B"; }

/* ========== Left stick (big circle on left) ========== */
.custom .sticks{
  position: absolute;
  left: 30px;
  top: 80px;
  width: 190px;
  height: 190px;
}

/* hide right stick */
.custom .stick.right{ display: none !important; }

/* Big outer circle (sketch look) */
.custom .sticks::before{
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  width: 170px;
  height: 170px;
  border-radius: 999px;
  border: 5px solid rgba(0,0,0,0.95);
  background: rgba(255,255,255,0.02);
  box-sizing: border-box;
}

/* stick dot (moves with analog input in many GV skins) */
.custom .stick.left{
  position: absolute;
  left: 85px;
  top: 85px;
  width: 18px;
  height: 18px;
  margin-left: -9px;
  margin-top: -9px;
  border-radius: 999px;
  background: rgba(0,0,0,0.85);
  box-shadow: 0 0 6px rgba(0,0,0,0.25);
}

/* If GV marks stick pressed, make it red */
.custom .stick.pressed.left{
  background: rgba(255,0,0,0.95);
  box-shadow: 0 0 14px rgba(255,0,0,0.85);
}
